---
'@g4os/agents': minor
---

Agent framework epic 07 (TASK-07-02): `@g4os/agents/claude` implementa `IAgent` via 9 módulos focados (total ~925 LOC vs 4716 em v1). `ClaudeProvider` é contrato injetável com três adapters (`DirectApiProvider`, `BedrockProvider`, `CompatProvider`) que lazy-carregam `@anthropic-ai/sdk` / `@aws-sdk/client-bedrock-runtime` / transport custom via `sdkFactory`. `ClaudeAgent extends DisposableBase` orquestra `AbortController` por sessão — dispose / interrupt / unsubscribe propagam o sinal até o provider. `StreamRunner` é `async function*` que traduz `ClaudeStreamEvent` → `AgentEvent` via `event-mapper` puro + `ToolUseAccumulator` (reconstrói JSON incremental de tool_use). `applyPromptCache1hTtl` marca o último bloco de `system` e `tools` com `cache_control: { type: 'ephemeral', ttl: '1h' }`; ativado automaticamente apenas em provider direct + modelos que declaram `promptCaching` (Opus/Sonnet/Haiku 4+, Claude 3.5). Bedrock SigV4 preservado — nenhum código muta o body pós-signing (bug v1 do fetch-interceptor eliminado). `createClaudeFactory({ resolveProvider })` suporta prefixos `anthropic*` / `claude*` / `bedrock-claude*` / `claude-compat*`. 45 testes cobrem todas as transformações puras + orquestração do runner + ciclo de vida do agent. Subpath export `@g4os/agents/claude`. ADR-0071.
