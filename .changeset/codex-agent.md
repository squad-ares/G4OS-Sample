---
'@g4os/agents': minor
---

Agent framework epic 07 (TASK-07-03): `@g4os/agents/codex` implementa `IAgent` via subprocess Codex CLI. 11 arquivos modulares (≤ 121 LOC cada): protocol/frame/subprocess/node-spawner/client/event-mapper/input-mapper/binary-resolver/bridge-mcp/codex-agent/factory. `SubprocessSpawner` é contrato injetável — default `NodeSubprocessSpawner` usa `node:child_process` (zero nova dep runtime); tests injetam fakes sem spawnar processos reais. `AppServerClient extends DisposableBase` gerencia stdin/stdout NDJSON com `LineBuffer` para chunks parciais e `jsonLineDecoder` gate que rejeita frames inválidos/malformados. Binary resolver tem DI para env/fileExists/bundledBinary com ordem `CODEX_DEV_PATH` > `CODEX_PATH` > bundled — cada passo valida file existence antes de usar. `CodexAgent` isola multi-turn via `requestId` filter; unsubscribe + interrupt enviam `{ type: 'cancel', requestId }` e retornam Result sem throw. `BridgeMcpConnector` é skeleton (attach/current/detach) com transport real aguardando TASK-08. 36 testes cobrem framing, event/input mapping, binary resolver (4 branches + error), AppServerClient chunked streams + lifecycle, CodexAgent multi-turn isolation + cancel + dispose, factory e bridge MCP. Biome override `noProcessEnv: off` para `**/codex/binary-resolver.ts`. Subpath export `@g4os/agents/codex`. ADR-0072.
